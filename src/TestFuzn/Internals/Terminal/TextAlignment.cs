namespace Fuzn.TestFuzn.Internals.Terminal;

/// <summary>
/// Horizontal alignment of text fitted into a fixed display width, e.g. a table cell.
/// </summary>
internal enum TextAlignment
{
    /// <summary>Text at the left edge, padding on the right.</summary>
    Left,

    /// <summary>Text at the right edge, padding on the left.</summary>
    Right
}
